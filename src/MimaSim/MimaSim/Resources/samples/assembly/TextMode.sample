macro clear(color) {
	unflag TEXTMODE
	flag CLEAR
	load color
	mov accumulator, dc
	unflag CLEAR
}

clear WHITE

macro drawChar(x, y, c) {
	flag TEXTMODE

	load x
	mov accumulator, dx

	load y
	mov accumulator, dy

	load c
	mov accumulator, dc
}

drawChar 0, 0, 'a'
drawChar 6, 0, 'b'