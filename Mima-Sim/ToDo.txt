VM:
	Mikroprogramme ausführen
	instruktionen ausführen
	
3 parser modi (raw, assembly, high (c-like language)

deploy as linux and windows standalone executable

GUI:
	BusRegistry
		Jeder Bus bekommt eindeutige ID und registriert sich in einer Registry
		In der Registry kann ich mehrere RegistryMaps definieren. Eine map enthält alle ids von Bussen die sie braucht.
		Wenn ein Bus aktiviert wird, werden alle Busse der map aktiviert bzw deaktiviert
		- in und output busse werden seperat behandeln in einigen komponenten, damit Dinge berechnet werden können


	Controls für verschiedene Einheiten:
		- Steuerwerk
	WrapperContainer für Componentent mit Bus,Register und Componente

	

	control hover ->
		popup mit informationen oder zoom was genau passiert


		
		